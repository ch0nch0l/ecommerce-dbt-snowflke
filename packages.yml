packages:
  # dbt_utils: essential macros used across every real dbt project
  # Provides: generate_surrogate_key, date_spine, pivot, star, etc.
  - package: dbt-labs/dbt_utils
    version: [">=1.0.0", "<2.0.0"]
